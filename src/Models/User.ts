export default interface UserType {
  id: string;
  firstName: string;
  lastName: string;
  gender: string;
  maritalStatus: string;
  birthdate: string;
  salary: number;
  active: boolean;
  comment: string;
}
